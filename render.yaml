services:
  - type: web
    name: glossary-app
    runtime: php
    plan: free
    buildCommand: "echo 'No build required'"
    startCommand: "php -S 0.0.0.0:10000 index.php"
    envVars:
      - key: PHP_VERSION
        value: "8.2"